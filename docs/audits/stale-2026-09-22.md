# Stale docs / likely-dead code scout — 2026-09-22

Wave 3 find-only inventory after Waves 1→2c (2026-09-21). No deletes, renames, or refactors in this lane.

Parent: WalksWithASwagger/cylon-detector#48 (kk-kb#3684).
Prior audit: [`stale-2026-09-21.md`](stale-2026-09-21.md) (Wave 1 / #40 / PR #41).

## Method

Repo-local evidence only. No live HTTP fetches, email, CMS, or Ads.

| Check | Evidence |
| --- | --- |
| Closed / open GitHub issues | MCP `list_issues`: open `#2`, `#9`–`#14`, `#48`; Wave 2/2b/2c `#42`/`#44`/`#46` closed completed; earlier closed set `#3`–`#8`, `#17`, `#22`–`#29`, `#40` unchanged |
| Merged PRs on `master` | `git log`: Wave 2 `#43`, Wave 2b `#45`, Wave 2c `#47` on `master` at `28eab03` (after `#1` + `#15`/`#16`/`#18`–`#21` + `#30`–`#39` + Wave 1 `#41`) |
| Wave 1 recheck | Re-read D1–D9 / C1–C8 surfaces; confirm deletes gone; confirm banners still true |
| Locale truth | `src/shared/site.ts` `AVAILABLE_LOCALES` (18) matches `index.html` inline `AVAILABLE` and `docs/i18n/progress/STATUS.json` (`not_started`: `pl` `pt-BR` `bn` `ko` `vi` `th`) |
| Internal paths | `rg` for `THEORY.md`, `i18n-wf-`, `appConfig`, `chartUtils`, `theory.scss`, `vite.svg`, `types/chart`; relative `[]()` links in scoped docs + README |
| Unused scripts / modules | `package.json` scripts vs `scripts/`; importers of each `src/` export; `public/` top-level assets |
| Theory-file census | `public/data/*.json` (211) vs `docs/i18n/progress/theory-chunks.json` unique filenames (210) vs `resolveTheoryFileStem` |
| “Current” claims post-2c | `rg` over `docs/**/*.md` for current / as of / remaining / land the PR |

Severity: `p0` broken in-repo link that agents would follow; `p1` misleading current-state claim; `p2` cleanup.

Wave 4 action is one of `update` | `delete` | `verify-with-human`.

Numbered Wave 1 rows D1–D9 / C1–C8 are **not** repeated as new work unless the Wave 2–2c fix is itself now wrong.

## Counts

| Bucket | Findings |
| --- | --- |
| Stale docs (new) | 2 |
| Likely-dead code (new) | 3 |
| **Total new this wave** | **5** |
| By severity | p0: 0 · p1: 1 · p2: 4 |
| Wave 1 still-open (not re-listed) | C5 (bannered; keys kept) |

Zero new findings would have been valid. This trunk is not clean.

---

## Stale docs

| ID | Path | Why stale | Severity | Wave 4 |
| --- | --- | --- | --- | --- |
| D10 | `docs/i18n/SPINUP.md` | Wave 2b D2 rewrite still says `` `scripts/i18n-wf-he.js` and `scripts/i18n-wf-id.js` are leftover one-shots for already-shipped locales ``. Wave 2c `#47` deleted both (`test -e` → gone; `glob scripts/i18n-wf*` → 0). The 2b sentence is now a current-state claim about files that are not in the tree. | p1 | update |
| D11 | `docs/i18n/TRANSLATION_GUIDE.md`, `docs/i18n/glossary.json` | Both say “211” theory source files. `ls public/data/*.json` is 211 only because it counts the unused space-named duplicate `Sheldrake Morphic.json` (C9). Pipeline truth is 210: `theory-chunks.json` unique filenames, SPINUP “Confirm all 210 files present”, and `resolveTheoryFileStem('Sheldrake Morphic')` → `Sheldrake-Morphic.json`. | p2 | update |

---

## Likely-dead code

| ID | Path | Why likely dead | Severity | Wave 4 |
| --- | --- | --- | --- | --- |
| C9 | `public/data/Sheldrake Morphic.json` | Byte-identical (`sha256` prefix `48188ae3f168e43a`) to live `public/data/Sheldrake-Morphic.json`. Only English-root copy; no locale dir has the space name. Not in `theory-chunks.json`. Loader (`src/utils/routing.ts` → `resolveTheoryFileStem`) never fetches it. Added `ee4576d` (2025-10-19) before hyphenated stems. | p2 | delete |
| C10 | `public/logo.png` | Tracked 13 KB raster. `rg logo.png` is zero across HTML/TS/SCSS/JSON/MD. Live mark is inline SVG in `src/templates/logo.html`. Same unused-asset class as Wave 1 C7 (`public/vite.svg`, deleted in `#47`). | p2 | delete |
| C11 | `public/Robert-Lawrence-Kuhn-headshot.webp` | Tracked 9 KB raster (`0ce22a3`). `rg Robert-Lawrence-Kuhn-headshot` / `headshot.webp` is zero. `paper.html` Kuhn links are external DOI/ScienceDirect, not this file. OG/twitter images use `banner.png`. | p2 | delete |

---

## Delta vs 2026-09-21 (`stale-2026-09-21.md`)

### Fixed (do not re-file)

| Wave 1 ID | Surface | Fix | Evidence |
| --- | --- | --- | --- |
| D1 | `docs/i18n/SPINUP.md` locale remaining list | Header “as of 2026-09-21”: shipped 18 match `AVAILABLE_LOCALES`; remaining `pl` `pt-BR` `bn` `ko` `vi` `th`; “No remaining RTL locales” | #44 / PR #45. Locale snapshot still matches STATUS.json + `index.html` `AVAILABLE`. |
| D2 | same file, machine-local Claude path | Step B points at `theory-chunks.json` + `scripts/i18n-check.js`; `~/.claude/projects/...` called out as not in tree | #44 / PR #45. Residual: D10 (deleted leftover-script sentence). |
| D3 | `docs/i18n/TRANSLATION_GUIDE.md` `THEORY.md` | Retargeted to `src/types/theory.ts`; `THEORY.md` documented as gitignored local-only | #42 / PR #43. Path still absent; guide no longer presents it as current schema. |
| D4 | same guide, layer-1 checker | `node scripts/i18n-check.js {locale}` | #44 / PR #45. `scripts/i18n-check.js` still present; `docs/i18n/progress/` still STATUS + chunks only. |
| D5 | `docs/operations/release-handoff.md` | Live receipt is `master` (#1 + #30–#39), not `codex/thursday-killer-alpha` | #44 / PR #45. Status paragraph still matches `28eab03`. |
| D6 | `docs/roadmap/field-release-workplan.md` | Workstream 1 / decision-queue item 1 marked **Done** in #1 | #44 / PR #45. Workstreams 2–7 remain human-gated field work. |
| D7 | `docs/roadmap/cylon-detector-roadmap.md` | Claim Ledger / Blind Chamber / receipt / three-voice marked landed; next work is field-prove | #44 / PR #45. Banner still matches `skunkworks-v2-tasks.md` `[x]`. |
| D8 | `docs/operations/invite-operations.md` | Dry-run `--expires-at 2027-12-31T00:00:00.000Z` plus “past timestamp mints an immediately expired invite” | #46 / PR #47. Copy-paste date is still in the future as of this scout. |
| D9 | apex vs `www` origin | README “Canonical site origin” chooses `https://www.consciousnessatlas.com`; apex is citation shorthand; DNS unchanged | #44 / PR #45. `SITE_ORIGIN`, sitemap, `index.html` / `paper.html` canonical still `www`. `bench.html` + i18n cite strings still use apex on purpose. |
| C1 | `scripts/i18n-wf-he.js` | Deleted | #46 / PR #47. Gone. Residual: D10. |
| C2 | `scripts/i18n-wf-id.js` | Deleted | #46 / PR #47. Gone. Residual: D10. |
| C3 | `src/utils/chartUtils.ts` | Deleted | #46 / PR #47. Gone; no importers. |
| C4 | `src/config/appConfig.ts` | Deleted | #46 / PR #47. Gone; no importers. |
| C6 | `src/pages/theory.scss` | Deleted | #46 / PR #47. Gone. `paper.scss` still imported by `paper.html`. |
| C7 | `public/vite.svg` | Deleted | #46 / PR #47. Gone. |
| C8 | `src/types/chart.ts` | Deleted | #46 / PR #47. Gone. |

No Wave 2–2c fix is itself wrong except **D10** (D2 leftover-script sentence after C1/C2 delete).

### Still open (Wave 1 IDs, not re-listed above)

| Wave 1 ID | Surface | Wave 3 note |
| --- | --- | --- |
| C5 | `.env.schema` `VITE_APP_*` / `VITE_CHART_*` / `VITE_API_BASE_URL` | Still unused after C4 delete. Wave 2c bannered each key “Unused Atlas leftover… kept as a public contract until a human confirms it can be dropped.” `rg` in `src/` finds no readers. No new evidence to drop or to promote to live config. Keep `verify-with-human`. |

### Newly found (this file)

D10, D11, C9, C10, C11.

---

## Out of scope

Noticed, not invented as Wave 4 work:

- **GitHub issue #2 body** (not a repo file) still says this repository has no `agentic/contract.json` or issue linter. False after #3 / #15. Coordination text, not a path this PR can edit.
- **Open field-release issues #9–#14** remain real human-gated work. Not stale because Wave 1–2c children closed.
- **`docs/specs/thursday-killer-alpha.md` and `docs/specs/skunkworks-v2.md`** are historical design records written in future tense. Left archival.
- **`docs/research/product-research-2026-07-20.md`** is a dated research receipt. “Current” there means July 2026 evidence.
- **`docs/voice-audit/slop-check-*.json`** are audit artifacts, not unused product code.
- **`fixtures/demo/witness-theory-adjudicated.json`** is the intentional v1 importer fixture (README + tests).
- **Agentic fixtures** that mention `#4` / `Closes #4` are contract tests, not open-work claims.
- **Remote leftover branches** (`origin/codex/thursday-killer-alpha` was already noted in Wave 1; current leftovers include Wave 2 working refs). GitHub refs, not tree paths.
- **`.gitignore` lists `/docs/i18n/SPINUP.md`** while the file remains tracked (`git ls-files`). Same hygiene note as Wave 1; no new evidence to untrack.
- **`scripts/generate-sitemap.js` comment** that `hi` is “half-translated” is still stale (Hindi is shipped). Logic (`getShippedLocales()` requires UI + taxonomy) still matches on-disk files. Comment-only; still not a finding.
- **Apex cite strings** in `bench.html` and `public/i18n/ui/*/about.footer.citeBody` stay citation shorthand per the D9 resolution. Not re-opened.
- **External literature URLs** in research notes and the roadmap were not fetched (no outbound).
- **Unused exports on live modules** (`PREFERRED_LOCALE_KEY`, `translatePage`, `MOCK_PROMPT_VERSION`, plus Wave 1’s `getCurrentPalette` / `getMobileLabelVisibility`) sit next to used code. Not listed.
- **No TODO/FIXME** in product TS/JS pointed at merged issues.
- **Dark-crystal ceremony / booth ritual drafts.** Other trunk; left alone.
- **No secrets** invented or recorded.

## Wave 4 hint

If a follow-up is authorized: update D10 first (runbook names deleted files), then delete C9 and align D11’s 211→210, then delete C10/C11 only after a human confirms they are not off-repo brand sources. Leave C5 bannered until a human drops the unused `VITE_*` contract keys. Do not treat this scout as delete authority.
