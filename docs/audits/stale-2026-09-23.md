# Stale docs / likely-dead code scout — 2026-09-23

Wave 5 find-only inventory after Waves 3→4b (2026-09-22 audit + D10/D11/C9 fixes). No deletes, renames, or refactors in this lane.

Parent: WalksWithASwagger/cylon-detector#54 (kk-kb#3693).
Prior audit: [`stale-2026-09-22.md`](stale-2026-09-22.md) (Wave 3 / #48 / PR #49).

## Method

Repo-local evidence only. No live HTTP fetches, email, CMS, or Ads.

| Check | Evidence |
| --- | --- |
| Closed / open GitHub issues | MCP `list_issues`: open `#2`, `#9`–`#14`, `#54`; Wave 4 `#50` / Wave 4b `#52` closed completed; Wave 3 `#48` closed; earlier closed set `#3`–`#8`, `#17`, `#22`–`#29`, `#40`, `#42`, `#44`, `#46` unchanged |
| Merged PRs on `master` | `git log`: Wave 3 `#49`, Wave 4 `#51`, Wave 4b `#53` on `master` at `20f5cf8` (after `#1` + `#15`/`#16`/`#18`–`#21` + `#30`–`#39` + Wave 1 `#41` + Wave 2 `#43` + Wave 2b `#45` + Wave 2c `#47`) |
| Wave 3 recheck | Re-read D10 / D11 / C9–C11 / C5 surfaces; confirm D10 banner, D11 210 census, and C9 delete; leave C10 / C11 / C5 as `verify-with-human` |
| Locale truth | `src/shared/site.ts` `AVAILABLE_LOCALES` (18) matches `index.html` inline `AVAILABLE` and `docs/i18n/progress/STATUS.json` (`not_started`: `pl` `pt-BR` `bn` `ko` `vi` `th`) |
| Internal paths | `rg` for `THEORY.md`, `i18n-wf-`, `appConfig`, `chartUtils`, `theory.scss`, `vite.svg`, `types/chart`, `Sheldrake Morphic.json`; relative `[]()` links in scoped docs + README (18 links, 0 missing) |
| Unused scripts / modules | `package.json` scripts vs `scripts/`; importers of each `src/` file; `public/` top-level assets |
| Theory-file census | `public/data/*.json` (210) = `docs/i18n/progress/theory-chunks.json` unique filenames (210) = `src/data/theoryNames.ts` keys (210) = `chartConfig.ts` leaf nodes (210) = `scripts/validate-data.ts` `EXPECTED_THEORY_PROFILES` (210). All 17 shipped locale dirs also 210. `resolveTheoryFileStem` maps every chart / theoryNames key onto an on-disk file. Zero space-named JSON under `public/`. |
| “Current” claims post-4b | `rg` over `docs/**/*.md` for current / as of / remaining / land the PR / `211` / `28eab03` |

Severity: `p0` broken in-repo link that agents would follow; `p1` misleading current-state claim; `p2` cleanup.

Wave 6 action is one of `update` | `delete` | `verify-with-human`.

Numbered Wave 1–3 rows D1–D11 / C1–C11 are **not** repeated as new work unless the Wave 4 / 4b fix is itself now wrong.

## Counts

| Bucket | Findings |
| --- | --- |
| Stale docs (new) | 0 |
| Likely-dead code (new) | 0 |
| **Total new this wave** | **0** |
| By severity | p0: 0 · p1: 0 · p2: 0 |
| Still-open verify-with-human (not re-listed) | C5, C10, C11 |

This trunk is clean. Zero new findings is the expected outcome after Waves 4 / 4b.

---

## Stale docs

None. Every Wave 3 doc row that Wave 4 / 4b was authorized to touch is now true, and no new broken link or post-4b current-state claim showed up.

## Likely-dead code

None. C9 (space-named Sheldrake duplicate) is gone. No leftover space-named JSON, no theory-loader mismatch, no orphaned script, no TODO/FIXME pointing at a merged issue.

---

## Delta vs 2026-09-22 (`stale-2026-09-22.md`)

### Fixed (do not re-file)

| Wave 3 ID | Surface | Fix | Evidence |
| --- | --- | --- | --- |
| D10 | `docs/i18n/SPINUP.md` leftover-script sentence | Step B now says the Hebrew/Indonesian one-shots (`scripts/i18n-wf-he.js`, `scripts/i18n-wf-id.js`) were deleted in `#47` and are not in this tree | #50 / PR #51. `glob scripts/i18n-wf*` → 0. Sentence is historical, not a current-state claim that the files exist. |
| D11 | `docs/i18n/TRANSLATION_GUIDE.md`, `docs/i18n/glossary.json` “211” | Both say 210 | #52 / PR #53. Matches `ls public/data/*.json` (210), `theory-chunks.json` unique filenames (210), SPINUP “Confirm all 210 files present”, and `EXPECTED_THEORY_PROFILES = 210`. |
| C9 | `public/data/Sheldrake Morphic.json` | Deleted | #52 / PR #53. `test -e` → gone. Hyphenated `public/data/Sheldrake-Morphic.json` remains; `resolveTheoryFileStem('Sheldrake Morphic')` still resolves to that stem. Validator bumped 211→210. |

No Wave 4 / 4b fix is itself wrong.

### Still open (Wave 1 / Wave 3 IDs, not re-listed above)

| ID | Surface | Wave 5 note |
| --- | --- | --- |
| C5 | `.env.schema` `VITE_APP_*` / `VITE_CHART_*` / `VITE_API_BASE_URL` | Still unused after C4 delete. Each key remains bannered “Unused Atlas leftover… kept as a public contract until a human confirms it can be dropped.” `rg` in `src/` / `api/` / `scripts/` finds no readers. No new evidence to drop or to promote to live config. Keep `verify-with-human`. |
| C10 | `public/logo.png` | Still tracked (13 KB). `rg logo.png` is still zero across HTML/TS/SCSS/JSON/MD. Live mark is still inline SVG in `src/templates/logo.html`. No new in-repo importer. Left `verify-with-human` per #52 / the Wave 5 gate (brand raster). |
| C11 | `public/Robert-Lawrence-Kuhn-headshot.webp` | Still tracked (9 KB). `rg Robert-Lawrence-Kuhn-headshot` / `headshot.webp` is still zero. `paper.html` Kuhn links remain external DOI/ScienceDirect. OG/twitter images still use `banner.png`. Left `verify-with-human` per #52 / the Wave 5 gate (brand raster). |

### Newly found (this file)

None.

---

## Out of scope

Noticed, not invented as Wave 6 work:

- **GitHub issue #2 body** (not a repo file) still says this repository has no `agentic/contract.json` or issue linter. False after #3 / #15. Coordination text, not a path this PR can edit.
- **Open field-release issues #9–#14** remain real human-gated work. Not stale because Wave 1–4b children closed.
- **`docs/specs/thursday-killer-alpha.md` and `docs/specs/skunkworks-v2.md`** are historical design records written in future tense. Left archival. Section 17’s case-collision families (`DOPS`/`Dops`, …) are resolved on disk (canonical `Dops.json`, `Mcfadden.json`, `Mcgilchrist.json`, `Mcginn.json`, `Nde.json`, `Qri.json` only); do not rewrite the spec unless a human wants it marked archival.
- **`docs/research/product-research-2026-07-20.md`** is a dated research receipt. “Current” there means July 2026 evidence.
- **`docs/voice-audit/slop-check-*.json`** are audit artifacts, not unused product code.
- **`fixtures/demo/witness-theory-adjudicated.json`** is the intentional v1 importer fixture (README + tests).
- **Agentic fixtures** that mention `#4` / `Closes #4` are contract tests, not open-work claims.
- **Remote leftover branches** are GitHub refs, not tree paths.
- **`.gitignore` lists `/docs/i18n/SPINUP.md`** while the file remains tracked (`git ls-files`). Same hygiene note as Waves 1 and 3; no new evidence to untrack. Same for ignore-only `TECHNICAL_DOCUMENTATION.md` / `GITHUB_LINK_GUIDE.md` / `CLAUDE.md` (absent, never tracked).
- **`scripts/generate-sitemap.js` comment** that `hi` is “half-translated” is still stale (Hindi is shipped; UI + taxonomy + 210 theory files present). Logic (`getShippedLocales()` requires UI + taxonomy) still matches on-disk files and `AVAILABLE_LOCALES`. Comment-only; still not a finding.
- **Apex cite strings** in `bench.html` and `public/i18n/ui/*/about.footer.citeBody` stay citation shorthand per the D9 resolution. Not re-opened.
- **Inherited SEO “325+” / “300+” copy** in `public/i18n/seo/*.json` is Atlas marketing for Kuhn’s landscape, not the digitized 210-file census that D11 / C9 repaired. Present in Waves 1 and 3; not a new post-4b doc drift. Changing 18 locale SEO strings is product copy, not Wave 6 audit cleanup.
- **External literature URLs** in research notes and the roadmap were not fetched (no outbound).
- **Unused exports on live modules** (`getCurrentPalette`, `getMobileLabelVisibility`; re-export of `PREFERRED_LOCALE_KEY`; stub `Router.getAllTheories()` that returns `[]`) sit next to used code. Same class as Wave 3; not listed. (`translatePage` and `MOCK_PROMPT_VERSION` are used in-file.)
- **README project-map line** `src/config/ Atlas app and chart config` still describes the directory after `appConfig.ts` was deleted in `#47`. Only `chartConfig.ts` remains. Wording nit, not a broken path.
- **No TODO/FIXME** in product TS/JS pointed at merged issues.
- **Dark-crystal ceremony / booth ritual drafts.** Other trunk; left alone.
- **No secrets** invented or recorded.

## Wave 6 hint

No new Wave 6 fix ticket is justified from this scout. The only remaining rows are C5 / C10 / C11, all still `verify-with-human` with no fresh evidence to reclassify. Do not treat this scout as delete authority.
