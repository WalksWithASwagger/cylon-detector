# Stale docs / likely-dead code scout — 2026-09-21

Wave 1 find-only inventory after the Sep 2026 issue/PR drain. No deletes, renames, or refactors in this lane.

Parent: WalksWithASwagger/cylon-detector#40 (kk-kb#3671).

## Method

Repo-local evidence only. No live HTTP fetches, email, CMS, or Ads.

| Check | Evidence |
| --- | --- |
| Closed / open GitHub issues | MCP `list_issues`: closed #3–#8, #17, #22–#29; open #2, #9–#14, #40 |
| Merged PRs on `master` | `git log --oneline`: #1, #15, #16, #18–#21, #30–#39 are on `master` at `17490de` |
| Locale truth | `src/shared/site.ts` `AVAILABLE_LOCALES` (18 shipped) vs `docs/i18n/progress/STATUS.json` vs `docs/i18n/SPINUP.md` |
| Internal paths | `rg` for `THEORY.md`, `docs/i18n/progress/`, `i18n-wf-`, `appConfig`, `chartUtils` |
| Unused scripts / modules | `package.json` scripts vs `scripts/`; importers of each `src/` export |
| “Current” claims predating 2026-09-01 | `rg` over `docs/**/*.md` for current / as of / remaining / land the PR |

Severity: `p0` broken in-repo link; `p1` misleading current-state claim; `p2` cleanup.

Wave 2 action is one of `update` | `delete` | `verify-with-human`.

## Counts

| Bucket | Findings |
| --- | --- |
| Stale docs | 9 |
| Likely-dead code | 8 |
| **Total** | **17** |
| By severity | p0: 1 · p1: 7 · p2: 9 |

---

## Stale docs

| ID | Path | Why stale | Severity | Wave 2 |
| --- | --- | --- | --- | --- |
| D1 | `docs/i18n/SPINUP.md` | Header still says “Current state (as of Malay completion)”: shipped 10 locales, remaining 14 including `it` `nl` `pt-PT` `ru` `tr` `he` `ja` `id`. Those eight are `done` / `passed` in `docs/i18n/progress/STATUS.json` and present in `AVAILABLE_LOCALES`. Actual remaining `not_started` locales are `pl` `pt-BR` `bn` `ko` `vi` `th`. Also claims “`he` is the only remaining RTL locale” after Hebrew shipped (`5c2b541`). | p1 | update |
| D2 | `docs/i18n/SPINUP.md` | Step B still points at a machine-local Claude workflow: `~/.claude/projects/-Users-dan-Repositories-c-atlas/<session>/workflows/scripts/translate-ms-theories-wf_*.js`. That path is not in this repository. | p1 | update |
| D3 | `docs/i18n/TRANSLATION_GUIDE.md` | Points at `src/data/THEORY.md` as the MTTS v5.0 schema. The path is listed in `.gitignore` and is absent (`src/data/` only has `theoryNames.ts`). In-repo link is broken. | p0 | update |
| D4 | `docs/i18n/TRANSLATION_GUIDE.md` | “Run the layer-1 validation script (see `docs/i18n/progress/`)” — that directory holds only `STATUS.json` and `theory-chunks.json`. The checker is `scripts/i18n-check.js`. | p1 | update |
| D5 | `docs/operations/release-handoff.md` | Status still treats `codex/thursday-killer-alpha` and “the pull request” as the live Git receipt (“Deliver the reviewed local diff through one pull request into `master`”). `master` already contains #1 plus the 2026-09-20 drain (#30–#39). Remote leftover branch `origin/codex/thursday-killer-alpha` is not the current delivery surface. | p1 | update |
| D6 | `docs/roadmap/field-release-workplan.md` | First decision queue item: “Review and land the current implementation pull request.” That landing happened in #1 (2026-07-20) and is no longer open work after the Sep 2026 drain. Workstreams 2–7 remain valid human-gated field work. | p1 | update |
| D7 | `docs/roadmap/cylon-detector-roadmap.md` | Still frames “the current alpha” and P0/P1 items (Claim Ledger, Blind Chamber, run receipt, three-voice view) as upcoming build. `docs/specs/skunkworks-v2-tasks.md` marks those `[x]`; README describes them as present in the local beta. Build-order line “next work is to land and field-prove them” is half-done: landed, not yet field-proven. | p1 | update |
| D8 | `docs/operations/invite-operations.md` | Dry-run examples use `--expires-at 2026-08-01T00:00:00.000Z`, a date that is already past as of this scout. Copy-paste would mint an immediately expired invite record. | p2 | update |
| D9 | `README.md`, `bench.html` vs `src/shared/site.ts` | Apex `https://consciousnessatlas.com` (README cite, bench credit) vs `https://www.consciousnessatlas.com` (`SITE_ORIGIN`, sitemap, `index.html` canonical). No live fetch in this lane. | p1 | verify-with-human |

---

## Likely-dead code

| ID | Path | Why likely dead | Severity | Wave 2 |
| --- | --- | --- | --- | --- |
| C1 | `scripts/i18n-wf-he.js` | One-shot Claude Workflow script. Not in `package.json`. No repo importers. Hardcodes `/Users/dan/Repositories/c-atlas/`. Hebrew content already shipped (`5c2b541`). | p2 | delete |
| C2 | `scripts/i18n-wf-id.js` | Same shape as C1 for Indonesian. Locale already shipped (`06736da`). | p2 | delete |
| C3 | `src/utils/chartUtils.ts` | `rg` finds no importer. Helpers (`calculateTotal`, `flattenData`, `findItem`, `generateRandomData`) are unused Atlas leftovers. | p2 | delete |
| C4 | `src/config/appConfig.ts` | No importer. Defaults still say `C-Atlas`, `1.0.0`, `http://localhost:3000` while `npm run dev` is Vite on 8080. Only consumer of `VITE_APP_TITLE`, `VITE_APP_VERSION`, `VITE_CHART_THEME`, `VITE_CHART_RENDERER`, `VITE_API_BASE_URL`. | p2 | delete |
| C5 | `.env.schema` (`VITE_APP_*` / `VITE_CHART_*` / `VITE_API_BASE_URL`) | Comments claim these are “shown by the inherited Consciousness Atlas client.” After C4, nothing in `src/` reads them. Keep the schema until a human confirms they are unused public contract, not live config. | p2 | verify-with-human |
| C6 | `src/pages/theory.scss` | No HTML/TS/SCSS import. `.theory-header` / `.theory-title` live in `src/components/ItemDetailsPanel.scss`. `paper.html` still imports `src/pages/paper.scss` (keep that). | p2 | delete |
| C7 | `public/vite.svg` | Tracked Vite scaffold logo. No references in HTML, TS, or CSS. | p2 | delete |
| C8 | `src/types/chart.ts` | `ChartConfig` / `ChartOptions` have no importers. `ChartDataItem` is only imported by unused `chartUtils.ts` (C3). | p2 | delete |

---

## Out of scope

Noticed, not invented as Wave 2 work:

- **GitHub issue #2 body** (not a repo file) still says this repository has no `agentic/contract.json` or issue linter. False after #3 / #15. Coordination text, not a path this PR can edit.
- **Open field-release issues #9–#14** remain real human-gated work. Not stale just because Wave 1 children closed.
- **`docs/specs/thursday-killer-alpha.md` and `docs/specs/skunkworks-v2.md`** are historical design records written in future tense. Do not rewrite unless a human wants them marked archival.
- **`docs/research/product-research-2026-07-20.md`** is a dated research receipt. “Current” there means July 2026 evidence, not a live status board.
- **`docs/voice-audit/slop-check-*.json`** are audit artifacts, not unused product code.
- **`fixtures/demo/witness-theory-adjudicated.json`** is the intentional v1 importer fixture (README + tests).
- **Agentic fixtures** that mention `#4` / `Closes #4` are contract tests, not open-work claims.
- **Remote leftover branches** (`origin/codex/thursday-killer-alpha`, `origin/codex/issue-*`) are GitHub refs, not tree paths.
- **`.gitignore` lists `/docs/i18n/SPINUP.md`** while the file remains tracked (`git ls-files` shows it). Hygiene only; do not untrack in Wave 2 without a human call.
- **`scripts/generate-sitemap.js` comment** that `hi` is “half-translated” is stale (Hindi is shipped), but the `getShippedLocales()` logic itself matches on-disk UI+taxonomy files. Comment-only; not listed as a finding.
- **External literature URLs** in the research notes and roadmap were not fetched (no outbound).
- **Unused exports on live modules** (`getCurrentPalette`, `getMobileLabelVisibility` in `chartConfig.ts`) are small and sit next to used palette code. Not listed.
- **No TODO/FIXME** in product TS/JS pointed at merged issues.
- **No secrets** invented or recorded.

## Wave 2 hint

If a follow-up is authorized: update D1–D4/D8 first (wrong locale/runbook facts), then D5–D7 (post-drain status), then delete C1–C4/C6–C8 only after C5 is confirmed. Do not treat this scout as delete authority.
